apiVersion: v1
kind: Service
metadata:
  name: ai-ops-agent-service
  namespace: ai-ops
spec:
  selector:
    app: ai-ops-agent
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
  type: NodePort