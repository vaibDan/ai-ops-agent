apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: ai-ops
spec:
  selector:
    app: sample-app
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
  type: NodePort